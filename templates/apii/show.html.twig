{% extends 'base.html.twig' %}



{% block body %}


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ apii.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ apii.montant }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ apii.telephone }}</td>
            </tr>
            <tr>
                <th>Operateur</th>
                <td>{{ apii.operateur }}</td>
            </tr>
            <tr>
                <th>Code_secret</th>
                <td>{{ apii.codeSecret }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_api_index') }}">back to list</a>

    <a href="{{ path('app_api_edit', {'id': apii.id}) }}">edit</a>

    {{ include('apii/_delete_form.html.twig') }}
{% endblock %}
